package _814_binary_tree_pruning

import org.junit.Assert.assertArrayEquals
import org.junit.Test

/**
 * Created by eth4 on 08/04/18.
 */

class Test814BinaryTreePruning {
    val tests = listOf(
            arrayOf(1,null,0,0,1)
    )
    val results = listOf(
            arrayOf(1,null,0,null,1)
    )

    @Test
    fun test814BinaryTreePruning() {
        val tests = tests
//        val data = tests.map { BinaryTreePruning().pruneTree(it) }
//        assertArrayEquals(results.toTypedArray(), data.toTypedArray())
    }

}
